# Claude Squad 프로젝트 파일별 기능 상세 분석

## 프로젝트 개요
Claude Squad는 여러 AI 어시스턴트(Claude Code, Aider, Codex, Gemini)를 동시에 관리하는 터미널 애플리케이션입니다. 각 AI 세션을 독립된 Git 워크트리와 tmux 세션에서 실행하여 충돌 없이 여러 작업을 병렬로 처리할 수 있습니다.

---

## 📁 루트 디렉토리 파일들

### `main.go` - 애플리케이션 진입점
**핵심 역할**: 전체 애플리케이션의 시작점이자 CLI 인터페이스 관리
**주요 기능**:
- CLI 명령어 정의 및 처리 (cobra 라이브러리 사용)
- 프로그램 플래그 처리 (`-p`, `-y`, `--daemon`)
- Git 저장소 유효성 검증
- 데몬 프로세스 생성/종료 관리
- 메인 앱 실행 (`app.Run()` 호출)

**중요 명령어들**:
- `cs`: 메인 애플리케이션 실행
- `cs reset`: 모든 인스턴스 및 설정 초기화
- `cs debug`: 설정 파일 경로 및 내용 표시
- `cs version`: 버전 정보 출력

### `go.mod` - Go 모듈 설정
**핵심 역할**: 프로젝트 의존성 관리
**주요 의존성**:
- `charmbracelet/bubbletea`: TUI(Terminal UI) 프레임워크
- `charmbracelet/lipgloss`: 터미널 스타일링
- `go-git/go-git`: Git 작업 라이브러리
- `spf13/cobra`: CLI 명령어 구조
- `creack/pty`: 가상 터미널 구현

### `.goreleaser.yaml` - 릴리스 자동화
**핵심 역할**: 멀티 플랫폼 바이너리 빌드 및 릴리스 자동화
**지원 플랫폼**: Linux, macOS, Windows (amd64, arm64)

---

## 📁 app/ - 메인 애플리케이션 로직

### `app/app.go` - 핵심 애플리케이션 제어
**핵심 역할**: Bubble Tea 기반 TUI 애플리케이션의 메인 모델
**주요 상태**:
- `stateDefault`: 기본 인스턴스 목록 화면
- `stateNew`: 새 인스턴스 생성 중
- `statePrompt`: 사용자 프롬프트 입력 중
- `stateHelp`: 도움말 화면
- `stateConfirm`: 확인 대화상자

**핵심 구조체 - `home`**:
```go
type home struct {
    ctx context.Context
    state state           // 현재 UI 상태
    instances []*session.Instance  // 활성 인스턴스 목록
    selectedIndex int     // 선택된 인스턴스 인덱스
    storage *session.Storage      // 데이터 영속성 관리
    // ... 기타 UI 컴포넌트들
}
```

### `app/help.go` - 도움말 시스템
**핵심 역할**: 사용자 도움말 렌더링
**기능**: 키보드 단축키 및 사용법 설명 화면 제공

---

## 📁 config/ - 설정 관리

### `config/config.go` - 애플리케이션 설정
**핵심 역할**: 사용자 설정 및 기본값 관리
**설정 구조체**:
```go
type Config struct {
    DefaultProgram      string  // 기본 AI 프로그램 (claude, aider 등)
    AutoYes            bool    // 자동 승인 모드
    DaemonPollInterval int     // 데몬 폴링 간격 (ms)
    BranchPrefix       string  // Git 브랜치 접두사
}
```
**설정 파일 위치**: `~/.claude-squad/config.json`

### `config/state.go` - 상태 관리
**핵심 역할**: 애플리케이션 런타임 상태 영속화
**기능**: 인스턴스 정보, 세션 상태 저장/로드

---

## 📁 session/ - 세션 관리 시스템

### `session/instance.go` - AI 인스턴스 추상화
**핵심 역할**: 개별 AI 세션의 완전한 추상화
**Instance 구조체 주요 필드**:
```go
type Instance struct {
    Title     string    // 사용자 정의 제목
    Path      string    // 워크스페이스 경로
    Branch    string    // Git 브랜치명
    Status    Status    // 현재 상태 (Running/Ready/Loading/Paused)
    Program   string    // 실행할 AI 프로그램
    AutoYes   bool      // 자동 승인 여부
    CreatedAt time.Time // 생성 시간
    UpdatedAt time.Time // 마지막 업데이트 시간
}
```

**주요 메서드**:
- `Start()`: 인스턴스 시작 (tmux 세션 + AI 프로그램 실행)
- `Stop()`: 인스턴스 중지
- `Attach()`: 세션에 연결
- `Detach()`: 세션에서 분리
- `Pause()`: 세션 일시정지 (워크트리 제거, 브랜치 보존)
- `Resume()`: 일시정지된 세션 재개

### `session/storage.go` - 데이터 영속성
**핵심 역할**: 인스턴스 정보의 파일 시스템 저장/로드
**주요 기능**:
- JSON 형태로 인스턴스 정보 직렬화
- 상태 파일 관리 (`~/.claude-squad/state.json`)
- 인스턴스 생성/삭제/수정 연산

---

## 📁 session/git/ - Git 워크트리 관리

### `session/git/worktree.go` - Git 워크트리 추상화
**핵심 역할**: 각 인스턴스별 독립된 Git 워크스페이스 관리
**GitWorktree 구조체**:
```go
type GitWorktree struct {
    repoPath      string  // 원본 저장소 경로
    worktreePath  string  // 워크트리 경로
    sessionName   string  // 세션명
    branchName    string  // 브랜치명
    baseCommitSHA string  // 기준 커밋 해시
}
```

**핵심 기능**:
- 독립된 워크트리 생성 (`~/.claude-squad/worktrees/` 하위)
- 세션별 브랜치 자동 생성
- 워크트리 생명주기 관리

### `session/git/worktree_ops.go` - Git 작업 수행
**핵심 역할**: 실제 Git 명령어 실행
**주요 기능**:
- 커밋 생성 및 메시지 자동화
- 원격 저장소 푸시
- 브랜치 관리
- 변경사항 스테이징

### `session/git/diff.go` - 변경사항 시각화
**핵심 역할**: Git diff 생성 및 UI 표시용 포맷팅
**기능**:
- 파일별 변경사항 추출
- 색상 코딩 적용
- 추가/삭제 라인 통계

### `session/git/util.go` - Git 유틸리티
**핵심 역할**: Git 관련 도우미 함수들
**주요 함수**:
- `IsGitRepo()`: Git 저장소 검증
- `GetCurrentBranch()`: 현재 브랜치 확인
- `CleanupWorktrees()`: 사용하지 않는 워크트리 정리

---

## 📁 session/tmux/ - 터미널 멀티플렉싱

### `session/tmux/tmux.go` - Tmux 세션 관리
**핵심 역할**: 각 AI 인스턴스를 독립된 tmux 세션에서 실행
**TmuxSession 구조체**:
```go
type TmuxSession struct {
    sanitizedName string    // tmux 세션명 (특수문자 제거됨)
    program       string    // 실행할 프로그램 (claude, aider 등)
    ptmx         *os.File   // PTY 마스터
    monitor      *statusMonitor  // 상태 모니터링
}
```

**핵심 기능**:
- tmux 세션 생성/삭제/연결
- AI 프로그램 자동 실행
- 실시간 출력 캡처
- 세션 상태 모니터링 (Ready/Running/Loading)

### `session/tmux/pty.go` - 가상 터미널 관리
**핵심 역할**: PTY(Pseudo Terminal) 생성 및 관리
**기능**:
- 가상 터미널 생성
- 입출력 스트림 관리
- 터미널 크기 조정

---

## 📁 daemon/ - 백그라운드 프로세스

### `daemon/daemon.go` - 자동화 데몬
**핵심 역할**: AutoYes 모드에서 AI 세션들을 자동으로 처리
**주요 기능**:
- 주기적으로 모든 활성 세션 확인
- AI 프롬프트 자동 승인
- 에러 처리 및 로깅
- 백그라운드에서 작업 진행

**동작 방식**:
1. 설정된 간격(기본값)으로 폴링
2. 각 세션의 상태 확인
3. Ready 상태인 세션에 자동으로 Enter 키 전송
4. 에러 발생 시 로그 기록

### `daemon/daemon_unix.go` / `daemon/daemon_windows.go` - 플랫폼별 구현
**핵심 역할**: 운영체제별 데몬 프로세스 관리
**기능**:
- 프로세스 시작/중지
- PID 파일 관리
- 시그널 처리

---

## 📁 ui/ - 사용자 인터페이스

### `ui/list.go` - 인스턴스 목록 UI
**핵심 역할**: 활성 인스턴스들의 시각적 목록 표시
**기능**:
- 인스턴스 상태별 아이콘 표시 (`●` Ready, `⏸` Paused)
- 선택된 항목 하이라이트
- 색상 코딩 (준비 상태: 초록색, 일시정지: 회색)
- Git 변경사항 통계 표시 (+추가/-삭제 라인 수)

### `ui/menu.go` - 명령어 메뉴
**핵심 역할**: 화면 하단 키보드 단축키 메뉴 렌더링
**MenuState별 표시**:
- `StateDefault`: 일반 세션 관리 키들
- `StateEmpty`: 인스턴스가 없을 때
- `StateNewInstance`: 새 인스턴스 생성 중
- `StatePrompt`: 프롬프트 입력 중

### `ui/preview.go` - 세션 미리보기
**핵심 역할**: 선택된 인스턴스의 터미널 출력 실시간 표시
**PreviewPane 구조체**:
```go
type PreviewPane struct {
    width  int
    height int
    viewport viewport.Model    // 스크롤 가능한 뷰포트
    previewState previewState  // 현재 표시 상태
}
```

**기능**:
- 터미널 출력 실시간 캡처
- 스크롤 지원
- ANSI 색상 코드 처리
- 폴백 메시지 표시

### `ui/diff.go` - Git Diff 표시
**핵심 역할**: 코드 변경사항 시각화
**기능**:
- 파일별 변경사항 그룹화
- 라인별 추가/삭제 색상 구분
- 컨텍스트 라인 표시
- 변경사항 통계

### `ui/tabbed_window.go` - 탭 인터페이스
**핵심 역할**: 미리보기와 Diff 화면 간 전환
**기능**:
- Tab 키로 화면 전환
- 현재 활성 탭 표시
- 탭별 독립적인 스크롤 상태

### `ui/overlay/` - 오버레이 컴포넌트
**핵심 역할**: 모달 대화상자 및 입력 폼
**포함된 컴포넌트**:
- 새 인스턴스 생성 폼
- 프롬프트 입력 대화상자
- 확인 메시지 박스
- 도움말 오버레이

---

## 📁 keys/ - 키보드 입력 처리

### `keys/keys.go` - 키 바인딩 정의
**핵심 역할**: 모든 키보드 단축키 정의 및 매핑
**주요 키 바인딩**:
```go
// 네비게이션
"↑/k": KeyUp        // 위로 이동
"↓/j": KeyDown      // 아래로 이동
"tab": KeyTab       // 탭 전환

// 세션 관리
"n": KeyNew         // 새 세션 생성
"N": KeyPrompt      // 프롬프트로 새 세션 생성
"D": KeyKill        // 세션 삭제
"Enter/o": KeyEnter // 세션 연결

// Git 작업
"c": KeyCheckout    // 체크아웃 (커밋 후 일시정지)
"r": KeyResume      // 일시정지된 세션 재개
"s": KeySubmit      // 커밋 및 푸시

// 기타
"q": KeyQuit        // 종료
"?": KeyHelp        // 도움말
```

---

## 📁 cmd/ - 명령어 실행 추상화

### `cmd/cmd.go` - 명령어 실행 인터페이스
**핵심 역할**: 외부 명령어 실행의 추상화 레이어
**Executor 인터페이스**:
```go
type Executor interface {
    Run(cmd *exec.Cmd) error      // 명령어 실행
    Output(cmd *exec.Cmd) ([]byte, error)  // 출력 캡처
}
```

**목적**: 테스트 가능성 확보 (Mock Executor 주입 가능)

---

## 📁 log/ - 로깅 시스템

### `log/log.go` - 다중 레벨 로깅
**핵심 역할**: 애플리케이션 전체의 로깅 시스템
**로그 레벨**:
- `InfoLog`: 일반 정보
- `WarningLog`: 경고 메시지
- `ErrorLog`: 에러 메시지

**로그 파일**: `/tmp/claudesquad.log`

---

## 📁 web/ - 웹 인터페이스

### `web/package.json` - 웹 앱 설정
**핵심 역할**: Next.js 기반 웹 애플리케이션 설정
**기술 스택**:
- React 19
- Next.js 15.3.2
- TypeScript 5

### `web/src/app/` - Next.js 앱
**핵심 역할**: 터미널 앱의 웹 버전 제공
**기능**: 브라우저에서 Claude Squad 기능 사용 가능

---

## 📁 .github/ - CI/CD 파이프라인

### `.github/workflows/build.yml` - 빌드 자동화
**핵심 역할**: 멀티 플랫폼 빌드 및 테스트
**지원 플랫폼**: Linux, macOS, Windows (amd64, arm64)

### `.github/workflows/release.yml` - 릴리스 자동화
**핵심 역할**: 태그 생성 시 자동 릴리스
**기능**: GoReleaser를 통한 바이너리 배포

### `.github/workflows/lint.yml` - 코드 품질 검사
**핵심 역할**: golangci-lint를 통한 코드 정적 분석

---

## 🔧 유틸리티 스크립트

### `install.sh` - 설치 스크립트
**핵심 역할**: 자동 설치 및 설정
**기능**:
- 최신 릴리스 다운로드
- 바이너리를 `~/.local/bin/` 에 설치
- 심볼릭 링크 생성 (`cs` 명령어)

### `clean.sh` / `clean_hard.sh` - 정리 스크립트
**핵심 역할**: 개발 환경 정리
**기능**:
- tmux 세션 종료
- 워크트리 제거
- 설정 파일 삭제

### `bump-version.sh` - 버전 관리
**핵심 역할**: 버전 번호 업데이트 및 태그 생성

---

## 💡 아키텍처 특징

### 1. 모듈러 설계
각 디렉토리가 명확한 책임을 가지며 의존성이 잘 정리되어 있습니다.

### 2. 플랫폼 독립성
Unix/Windows별 구현이 분리되어 있어 크로스 플랫폼 지원이 용이합니다.

### 3. 테스트 가능성
인터페이스 기반 설계로 단위 테스트가 용이합니다.

### 4. 확장성
새로운 AI 프로그램 추가가 설정 변경만으로 가능합니다.

### 5. 상태 관리
세션 상태가 파일로 영속화되어 재시작 후에도 복원됩니다.

이러한 구조를 통해 Claude Squad는 복잡한 멀티 세션 관리를 직관적이고 안정적으로 제공합니다.